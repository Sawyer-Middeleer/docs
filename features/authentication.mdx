---
title: "Authentication"
description: "Managed authentication in BrowserAI"
icon: "user-check"
---

BrowserAI serves as a secure gateway for trusted AI agents to access business applications via browser use.

## Credential vault

```markdown
Insert screenshot
```

## Authentication flow automation

```markdown
Insert screenshot
```

We store cookies for your browser sessions by default, minimizing the need for re-authentication. If re-authentication is needed, we notify the appropriate user to refresh credentials.

## Supported auth methods

- Password authentication

## Coming soon

- OAuth
- Multi-factor Authentication (MFA)
- Single Sign-On (SSO)
