---
title: "Quickstart"
description: "Create your first browser automation MCP server"
---

Create your first browser automation MCP server

```markdown
insert diagram
```

```markdown
insert video
```

### **STEP 1: Configure Authentication**

- Provide credentials and app auth URL
- Describe authentication workflow using natural language
- Test and validate the complete workflow

```markdown
CTA to auth deep dive
```

```markdown
CTA to auth best practices
```

### **STEP 2: Define a Skill**

- Describe the task steps using natural language
- Test and validate the complete workflow

```markdown
CTA to automation deep dive
```

```markdown
CTA to automation best practices
```

### **STEP 3: Create MCP or API tool**

- BrowseAI automatically creates a hosted endpoint once your automation is saved
- The endpoint is a pre-configured MCP tool
- You can also send `POST` requests to the endpoint to trigger it

### **STEP 4:**

- Integrate with your application
  - Claude
  - ChatGPT
  - n8n
  - Langchain
  - CrewAI

```markdown
CTA to tool integration deep dive
```
